# 비밀 값 — 절대 커밋 금지
.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# 대시보드 실제 설정 (경로/라벨이 이 서버 전용)
dashboard/config/config.yaml

# Homepage — 업스트림 오픈소스를 통째로 clone+build한 것, 커밋 안 함 (README 설치 절차로 재현)
dashboard/homepage/
.pnpm-store/
