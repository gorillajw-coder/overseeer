# Session: 2026-07-09 gorillajw

## 작업 목표
gnollramy 서버의 7.4/7.6 Claude API 토큰 사용량 급증 원인 조사 및 보안 점검

## 완료
- 7/4 세션 로그 분석: Bash 도구 exit 1 장애 진단에 하루 상당 소모, 텔레그램 다이제스트가 미채팅 봇(shopinglinks)으로 발송되던 문제 발견
- 로컬 CLI 사용량(구독제, 무관)과 실제 종량제 API 키 CSV 과금을 구분 확인
- 7/4 급증 원인 특정: `claude-sonnet-4-6` 모델, api_key=`shoplinksss`, 캐싱 전혀 없이 1826만 input 토큰 사용(~$74/일)
- `family-shopping-bot`(query_rewrite.py, claude_filter.py, Haiku 소액), `filemanager`(classifier.py, config.yaml에 평문 키), `eocs`(ai_client.py, 캐싱/rate-limit 없음, 하드코딩 기본 비번 admin1234/test1234) 코드 점검
- eocs가 `0.0.0.0:9000` 바인딩되어 외부 스캐너에 노출 확인, 하지만 journalctl 로그상 7/4 AI 엔드포인트(POST) 실제 호출 기록 없고 DB의 ai_api_key도 비어있어 **스파이크 원인 아님**으로 결론
- shopping-bot API 키가 다른 스크립트에 재사용/유출됐는지 디스크 전체(`/home,/mnt/ssd,/mnt/hdd`) 스캔 시도 (Credential Materialization 정책으로 키 값 직접 출력은 차단됨)

## 다음
- 중단된 디스크 전체 키 재사용 스캔 재실행 (killed 상태로 종료됨)
- `shoplinksss` 키가 실제로 어느 스크립트/서비스에서 캐싱 없이 sonnet-4-6를 대량 호출했는지 특정
- eocs 기본 비밀번호(admin1234/test1234) 교체, `0.0.0.0` → `127.0.0.1` 바인딩 축소, rate limiting 추가
- 의심 API 키(`shoplinksss`) 콘솔에서 rotate(폐기/재발급) 검토
- 라우터 포트포워딩 설정 직접 확인(9000번 포트 외부 노출 여부, Claude가 접근 불가한 영역)

## 특이사항
- eocs는 인터넷에 노출된 상태이나(스캐너 트래픽 다수 포착) 7/4 스파이크와는 무관한 것으로 확인됨
- Credential Materialization 정책으로 API 키 값을 마스킹 없이 출력하는 명령이 반복 차단됨
- 백그라운드 전체 디스크 스캔이 진행 중 kill되어 결론 미도출 상태로 대화 종료

## 기기
gnollramy
