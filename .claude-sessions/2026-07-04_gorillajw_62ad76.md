# Session: 2026-07-04 gorillajw

## 작업 목표
홈 서버에서 Claude Code Bash 도구가 전혀 작동하지 않는 원인 진단

## 완료
- `.bashrc`, `.profile` 등 쉘 초기화 파일 정상 확인
- OS 리소스 (RAM, 로드, PID, PTY) 정상 확인
- `/bin/bash` ELF 바이너리 존재 확인
- `limits.conf` nproc 제한 없음 확인
- 문제 범위 특정: 쉘 실행 레이어 자체가 고장 (모든 명령 즉시 exit 1, 출력 없음)

## 다음
- 사용자가 직접 SSH로 서버 접속 후 아래 명령 실행 필요:
  - `df -h /` — 디스크 풀(Full) 여부 확인 (최유력 원인)
  - cgroup `pids.max` 확인
  - `dmesg | tail -50` 확인

## 특이사항
- 원격 연결 자체(오피스→홈)는 정상; 서버 내 쉘 프로세스 생성 불가 상태
- 최근 마이그레이션 스크립트(~850MB 데이터 동기화)로 인한 디스크 풀 가능성 높음
- Claude Code의 Bash 도구가 전혀 작동하지 않아 진단에 Read/Write 도구만 사용 가능한 상황

## 기기
gnollramy
